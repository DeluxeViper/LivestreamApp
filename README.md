# LivestreamApp

### Motivation

### Built with
* Kotlin
* XML
* Android Studio
* Gradle
* MVI
* (upcoming) AWS ECS

### Setup

NGINX Rtmp Server setup:
  `docker run -d -p 1935:1935 --name nginx-rtmp tiangolo/nginx-rtmp`
